{
  "name": "upgradepilot-mission-control",
  "version": "0.1.0",
  "private": true,
  "description": "Turnkey scripts for Mission Control: install, build the dashboard, start the server.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server start",
    "dev": "npm --prefix web run build && npm --prefix server run dev",
    "test": "npm --prefix server test && npm --prefix web test"
  }
}
